# Q-CABS：面向硬标签模型抽取的 NISQ 原生量子委员会查询算法

## 1. 论文的量子核心

本文不把量子线路作为经典主动学习器的装饰，也不宣称小规模量子电路能够直接复制一个高容量 CNN、LSTM 或 LLM。我们的中心命题是：**在固定硬标签预算下，有限 shots 的量子委员会能否提供一种与经典神经代理不同的、可部署在 NISQ 设备上的查询排序归纳偏置。**

因此，量子模块负责“选择什么去问”，经典 clone 负责“如何用已问到的标签复制行为”。这种分工与当前可用量子比特数和经典 API 的物理接口一致。

## 2. 物理合法的量子委员会

给定原始输入 `x`，攻击者先用固定公开映射得到低维向量 `z=P_q(x) in R^d`。第 `m` 个委员会成员由 `n` 比特、`L` 层参数化量子线路组成：

`U_m(z;theta_m)=prod_(l=0)^(L-1) [ E_Gl V(theta_(m,l)) D_l(z) ]`。

其中，数据编码层为

`D_l(z)=prod_(j=0)^(n-1) RY(s_l z_((j+ln) mod d)) RZ(s_l z_((j+ln) mod d)^2/pi)`，

`s_l=1/sqrt(l+1)` 是层相关缩放。该**特征循环数据重上传**使 `n` 个物理比特可在 `L` 层覆盖至多 `nL` 个投影特征，而不把高维经典向量错误地声称为可无代价振幅编码。

`V(theta)` 是每个比特上的可训练 `RX-RY-RZ` 旋转；`E_G` 是 `linear`、`circular`、`star` 或 `full` 图上的 CNOT 纠缠层。每一层均为幺正变换。含噪执行时，`E_G V D` 后施加局部 CPTP 通道 `N_p`，例如相位翻转、比特翻转或振幅衰减。因而整个演化为完全正且保迹的量子信道：

`rho_m(z)=N_p o U_m(z) rho_0 U_m(z)^dagger`。

这一定义排除了“量子态坍缩泄露目标训练数据”或“纠缠伪装经典 API 请求”等不符合本威胁模型的叙述。

## 3. 关联读出与有限 shots 概率预测

每个成员测量单体及关联可观测量：

`r_m(z)=[<Z_1>,...,<Z_n>,<Z_1Z_2>,...,<Z_(n-1)Z_n>]`。

由线性读出头得到硬标签代理的 logits：

`a_m(z)=W_m r_m(z)+b_m,   p_m(y|z)=softmax(a_m(z))`。

在真实设备上，`r_m` 由 `S` 次测量估计。对任一 Pauli 可观测量 `O_k in {-1,+1}`，Hoeffding 不等式给出

`Pr(|rhat_(m,k)-r_(m,k)|>epsilon) <= 2 exp(-2S epsilon^2)`。

对 `K` 个读出项取并集界，置信度 `1-delta` 下有

`epsilon_S(delta)=sqrt(log(2K/delta)/(2S))`。

该量是 Q-CABS 的**shots 证书**：它不是声称分类一定正确，而是量化排序时测量误差可能改变候选不确定性的程度。

## 4. 量子委员会辅助边界采样

设 `M` 个独立初始化的量子线路构成委员会，平均预测为

`pbar(z)=1/M sum_m p_m(z)`。

我们定义三个可解释量：

1. **边界不确定性**：`U(z)=1-[pbar_(1)(z)-pbar_(2)(z)]`。它优先选择委员会平均预测接近决策边界的候选。
2. **量子委员会分歧**：`D_Q(z)=1/M sum_m ||p_m(z)-pbar(z)||_2^2`。它刻画不同参数化量子特征映射对同一候选的测量分布是否产生不一致判断。
3. **经典覆盖度**：`C(z)=min_(x_i in S_Q) ||P_q(z)-P_q(x_i)||_2`。它避免所有查询集中在同一个局部边界。

对已查询集合 `S_Q` 和候选池 `P`，Q-CABS 的选择分数为

`Score(z)=alpha U(z)+beta D_Q(z)+gamma C(z)-eta R_S(z)`，

其中 `alpha+beta+gamma=1`，`R_S(z)` 是 shots 风险项。可取

`R_S(z)=epsilon_S(delta)/(pbar_(1)(z)-pbar_(2)(z)+epsilon_0)`。

这使高不确定性候选不会因纯测量噪声而被误认为边界点。只有当委员会分歧、边界不确定性和覆盖度共同较高，并且 shots 证书允许时，样本才被发送到经典 victim API。

## 5. 为什么它是量子方法，而不是普通 ensemble

Q-CABS 的量子性不在“使用多个模型”这一点，而在三个不可被实现细节抹去的对象：

1. 每个成员的表示来自纠缠线路对 Pauli 关联的测量，而不是一个经典隐藏层。
2. 分歧 `D_Q` 是由多条量子线路的测量概率分布得到，且可随编码、纠缠拓扑、shots 和物理噪声连续变化。
3. 排序决策接受有限 shots 误差证书约束，因此可以在 IBM 类硬件上重复并报告电路深度、物理比特映射和 job ID。

这并不自动构成复杂度意义上的量子优势。论文唯一可检验的经验主张是：在固定预算和最终 clone 下，Q-CABS 是否在某些任务区域稳定降低 `Q_tau`，或在相同 `Q` 下提升 clone fidelity。

## 6. 命题与可证伪预测

**命题 1（物理可实现性）。** 在每个噪声层为 CPTP 映射、测量为 Pauli 测量的条件下，Q-CABS 的量子特征、shots 估计和查询分数均可由标准 NISQ 设备实现。  
**证明纲要。** 幺正演化与 CPTP 映射的复合仍为 CPTP；Pauli 测量产生有界随机变量，故其经验均值满足上述有限 shots 界。

**命题 2（硬标签边界）。** Q-CABS 不会增加目标接口一次响应所携带的信息；`Q` 次 `C` 类硬标签响应至多提供 `Q log2(C)` 比特标签信息。  
**含义。** 本文不能且不应主张仅靠量子测量精确恢复任意目标参数或私有训练集。

**可证伪预测 P1。** 在相同最终 MLP clone、预算和 seed 下，Q-CABS 的 `Q_tau` 小于 Classical-Active 的 `Q_tau`，且其配对 95% 置信区间不跨零。

**可证伪预测 P2。** 当 shots 降低或噪声升高时，未使用 `R_S` 的量子委员会排序退化更快；加入 shots 风险项可改善排序保持率或最终 fidelity。

**可证伪预测 P3。** 增加 qubits 不保证改进；性能应在中等深度/中等比特数附近出现任务相关最优点。若结果单调或不稳定，应报告而非选择性隐藏。

## 7. 实验必须回答的量子问题

| 问题 | 必须的图/表 | 判定标准 |
|---|---|---|
| Q-CABS 是否有效？ | Q_tau 与 F-Q 曲线 | 对 Classical-Active 的配对 CI 为正 |
| 量子关联是否有用？ | Z 与 Z+ZZ 消融 | 关联项带来可重复增益 |
| 委员会是否有用？ | M=1/3/5 消融 | 分歧项优于单线路不确定性 |
| shots 证书是否有用？ | 有/无 R_S 的 shots 曲线 | 低 shots 下排序更稳定 |
| NISQ 上能否复现？ | 噪声热图和 IBM 回放表 | 报告真实 job ID 与校准 |
| 是否优于强经典法？ | 森林图和 QEDG 表 | 不低于强基线才讨论优势 |

## 8. 当前结论的正确表述

现有实验尚未证明 P1。它们仅说明独立小 QNN clone 不具竞争力，以及特定有限 shots replay 在模拟器下未立即崩溃。论文的下一阶段必须以 Q-CABS 查询效率而非独立 QNN fidelity 为主结果；若 P1 失败，仍可将论文定位为 NISQ 量子查询代理的能力边界研究。
