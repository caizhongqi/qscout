# 4. 实验

本节检验的不是“量子线路能否神奇地恢复模型参数”，而是一个更可证伪的问题：在相同的硬标签查询预算、候选池、最终 clone 容量和随机种子下，Q-Scout 的量子查询代理是否能比随机查询及强经典主动查询更有效地选择查询样本；这种效应在含噪 NISQ 条件和接口防御下是否仍然存在。除特别说明的 pilot 外，本文不将尚未完成的实验矩阵写成既有结果。

## 4.1 实验问题

我们围绕六个问题组织实验。

**RQ1（主效应）**：在五个公开数据集、四类经典 victim 和多个查询预算下，Q-Scout 是否提高最终经典 clone 对干净 victim 的功能一致率？

**RQ2（强基线）**：相对随机查询、经典不确定性-多样性主动学习以及外部数据自由攻击 QEDG，Q-Scout 是否能以更少查询达到给定一致率阈值？

**RQ3（量子设计）**：量子比特数、层数、特征循环、纠缠拓扑与关联测量分别贡献多少？性能是否存在容量饱和或退化，而不是简单随比特数增长？

**RQ4（NISQ 鲁棒性）**：有限 shots、相位翻转、比特翻转与振幅衰减对查询选择和最终 clone 有何影响？受控模拟结果能否由真实 IBM 后端回放验证？

**RQ5（防御边界）**：当接口采用硬标签随机化、查询限额和内容型 OOD 检测时，攻击的一致率、检测率和正常服务效用如何权衡？

**RQ6（成本与泛化）**：算法在不同数据模态、不同 victim 架构下是否稳定；达到同一功能一致率需要多少 API 查询、量子线路评估和壁钟时间？

## 4.2 数据集、victim 与攻击协议

我们使用两个图像数据集 MNIST、FashionMNIST，以及三个 UCR/UEA 时间序列数据集 FordA、Wafer、ElectricDevices。图像 victim 为 CNN 与 MLP；时间序列 victim 为 1D-CNN 与 LSTM。对每个数据集仅保留测试准确率达到预设门槛的 victim；未训练充分的 victim 不进入方法比较，避免把受害模型错误当作攻击成功。

攻击者可访问与任务同分布的公开无标签候选池，并只能获得 `top-1` 硬标签。攻击者不能访问概率、梯度、参数、结构或训练数据。Q-Scout 只在 PCA 压缩后的低维表示上运行量子查询代理；所有最终经典 clone 均在同一份高保真原始表示上训练。这个双表示协议避免把 QNN 的比特资源约束不公平地施加给随机、经典主动学习或 QEDG。

主指标为干净测试集上的功能一致率

`F = Pr[g(x) = O_f(x)]`。

我们同时报告 victim-correct fidelity、相对多数类基线的增益 `F-F_major`、达到阈值 `tau` 所需查询数 `Q_tau`、总 API 查询数、选择阶段耗时、QNN 拟合耗时和总耗时。只有同时超过多数类基线且在与强经典基线的配对比较中具有正向置信区间时，才将某个结果解释为有效的查询选择收益。

## 4.3 对照方法与公平性

每个单元比较以下方法。

| 方法 | 查询选择 | 最终 clone | 目的 |
|---|---|---|---|
| Random | 候选池均匀采样 | 容量匹配 MLP | 最低成本参考 |
| Classical-Active | MLP 边界不确定性与欧氏多样性 | 相同 MLP | 强非量子主动学习对照 |
| QEDG | 官方 AAAI 2025 实现的数据自由生成 | 与原论文及本研究匹配的 clone | 外部硬标签窃取基线 |
| Q-Scout | QNN 边界不确定性与多样性 | 相同 MLP | 待检验方法 |

QEDG 必须从其官方代码库独立复现，并按相同 victim、数据划分、查询预算和评价指标重新报告；任何自写生成策略均不得标记为 QEDG。对于 Random、Classical-Active 与 Q-Scout，候选池、初始随机查询、查询预算、最终 clone、训练轮数和评估样本完全一致。每个配置使用种子 `{7, 19, 31, 43, 59}`；Q-Scout 与 Classical-Active 采用相同 seed 的配对差值，而非比较两个无关均值。

## 4.4 主结果：查询效率矩阵

主实验覆盖五个数据集、适用的四类 victim、五个硬标签预算 `{64, 128, 256, 512, 1024}`、五个随机种子和三种内部方法。每个单元输出逐 seed 原始 CSV、均值/标准差 CSV，以及 QNN-guided clone 减去 Classical-Active clone 的配对表。论文主表应按预算报告 `F` 和 `F-F_major`，主图应绘制 `F-Q` 曲线与 `Q_tau` 柱状图。

统计时先对每个 seed 计算配对差值 `Delta_s=F_QScout,s-F_Classical,s`，再报告 `mean(Delta)`、标准差、95% 置信区间及 win/tie/loss 计数。若 95% 区间跨越零，则仅报告“未观察到稳定优于经典主动学习的证据”；不得以个别 seed 的正增益声明量子优势。

当前已完成的 MNIST CNN pilot（256 个硬标签、单 seed）表明 Random+MLP 为 0.798，Q-Scout-guided+MLP 为 0.804，独立小型 QNN 为 0.102。该差值仅为 0.006，且没有多 seed 置信区间，因此只能作为可运行性信号，不构成 RQ1 的结论。

## 4.5 量子消融与 NISQ 实验

容量消融扫描量子比特 `n in {2,3,4,5}`、层数 `L in {1,2,3}` 和编码特征数 `d in {8,12,16,20}`。在开发阶段还分别关闭特征循环、数据重上传和 `ZZ` 关联测量，并比较 `none`、`linear`、`circular`、`star` 与 `full` 纠缠拓扑。选择最终配置时使用开发集而非主测试集；论文同时报告性能、总线路评估次数和运行时间，避免把更大线路误解为无成本改进。

NISQ 鲁棒性在同一已训练参数上扫描相位翻转、比特翻转和振幅衰减，错误率为 `{0.002, 0.005, 0.01}`。有限 shots 取 `{128, 512, 1024, 4096}`，并与解析期望值比较。真实硬件部分只在获得 IBM Quantum job ID 后报告，必须附带后端名称、执行日期、校准快照、物理比特映射、transpilation depth、shots 和误差缓解设置；模拟器结果不能替代真实 QPU 结果。

## 4.6 防御、成本与报告规范

我们首先实现可控的硬标签随机化：攻击阶段每次响应以概率 `p in {0.01,0.05,0.10}` 替换为错误类别，最终评估始终对干净 victim 进行。该设置刻画输出扰动的攻击-效用边界，但不等同于差分隐私保证。后续应加入 API 查询限额、PRADA 类序列检测和 FlowGuard 类内容检测，并在相同正常服务准确率约束下报告检测率与 `Q_tau`。

每行实验日志保存 `query_count`、`selection_seconds`、`qnn_fit_seconds` 与 `total_seconds`。成本表必须将量子线路选择阶段与最终经典 clone 训练阶段分开；真实硬件时还应报告提交到完成的墙钟时间和每个 job 的 shots。所有失败单元、被低质量 victim 门槛跳过的单元、以及多数类基线未通过的单元均需保留在附录，不得删除。

可执行入口为：

```powershell
& "D:\ProgramData\py2\python.exe" run_study_matrix.py --mode main
& "D:\ProgramData\py2\python.exe" run_study_matrix.py --mode ablations
& "D:\ProgramData\py2\python.exe" run_study_matrix.py --mode noise
& "D:\ProgramData\py2\python.exe" run_study_matrix.py --mode defense
```

这套实验设计的判定标准是严格的：若 Q-Scout 未能稳定胜过 Classical-Active 与 QEDG，则论文应把结果表述为硬标签抽取中量子代理的能力边界、NISQ 退化与防御响应，而非宣称未被数据支撑的量子攻击优势。
