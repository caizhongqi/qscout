# 5. 实验结果与分析（基于已保存的真实输出）

本节只使用 `outputs/` 中已保存的 CSV。它不是完整主实验的替代品：多数单元只有一个随机种子，部分公共时间序列 victim 未达到可靠训练质量，且尚未接入 QEDG。因此，本节的价值是界定当前方法的能力边界和下一轮实验的优先级，而不是提前声称量子攻击优势。

## 5.1 结果可用性与解释规则

表 1 将所有现有输出分为可解释、仅作工程验证和不可作为攻击证据三类。三个规则贯穿下文：

1. **受害模型门槛**：victim 准确率过低时，和 victim 的高一致率可能只是复制错误或多数行为。
2. **多数类门槛**：只有功能一致率高于测试集多数类比例，才可能表明提取到了非平凡决策边界。
3. **强基线门槛**：若 QNN 不超过匹配的经典 clone，则不能主张量子查询优势。

| 输出文件 | 可用问题 | 当前证据级别 | 主要限制 |
|---|---|---|---|
| `qscout_runner_smoke.csv` | 硬标签端到端链路 | 工程 smoke test | 单 seed、32 查询 |
| `active_hardlabel_benchmark.csv` | MNIST 纯 QNN 与经典 clone | 能力边界 | 单 seed、无配对主动策略 |
| `torch_benchmark_results.csv` | 图像/长序列的经典对照 | 可解释的负结果 | 合成数据、单 seed |
| `public_datasets_smoke_results_20260618.csv` | 五公开数据集加载与训练 | 工程验证 | 训练轮数过少，存在欠训练/失衡 |
| `hardware_simulation_results.csv` | 有限 shots 噪声 replay | 受控模拟 | 不是 IBM 真机 job |

## 5.2 硬标签 smoke test：独立 QNN 未通过有效性门槛

表 2 是当前可复核的 MNIST CNN smoke test。QNN 代理的一致率为 0.100，低于多数类基线 0.150；即使最终使用同一批硬标签训练经典 MLP clone，功能一致率也只有 0.640。该实验说明管线能够运行，但不能证明 QNN 提取有效。

**表 2：MNIST CNN，32 个硬标签查询，3 qubits/1 layer，单 seed。**

| 指标 | 数值 |
|---|---:|
| Victim 测试准确率 | 0.990 |
| 多数类基线一致率 | 0.150 |
| 独立 QNN 一致率 | 0.100 |
| QNN 相对多数类增益 | -0.050 |
| Logistic clone 一致率 | 0.670 |
| MLP clone 一致率 | 0.640 |
| QNN 是否通过多数类门槛 | 否 |

这里的关键结论不是“攻击失败所以项目无意义”，而是：低预算下用小 VQC 直接充当高保真 clone 的假设不成立。后续论文必须把 QNN 的角色限制为查询代理，并以 QNN-guided clone 与 Classical-Active clone 的**配对差值**作为主指标。

## 5.3 公共数据集 pilot：高数值不等于攻击成功

表 3 列出五公开数据集的旧 smoke 输出。MNIST/FashionMNIST 的 QNN 一致率仅 0.161--0.186；ElectricDevices victim 本身准确率为 0.381--0.551；FordA LSTM 被日志明确标记为欠训练。Wafer 的 0.920 和 1.000 虽然表面很高，但没有多数类基线、类别频率和强经典 clone 作为参照，其中 LSTM 结果已被标为“likely majority behavior”。因此这些数字只能证明数据管线可运行，不能进入论文主结果表。

**表 3：五公开数据集 early smoke（256 查询；不可作为主结论）。**

| 数据集 | Victim | Victim 准确率 | QNN 一致率 | 审稿级解释 |
|---|---|---:|---:|---|
| MNIST | CNN | 0.978 | 0.168 | 失败；远低于可用 clone 水平 |
| MNIST | MLP | 0.920 | 0.185 | 失败 |
| FashionMNIST | CNN | 0.875 | 0.161 | 失败 |
| FashionMNIST | MLP | 0.843 | 0.186 | 失败 |
| FordA | 1D-CNN | 0.673 | 0.809 | victim 未达质量门槛，不可解释 |
| FordA | LSTM | 0.514 | 0.995 | 欠训练，排除 |
| Wafer | 1D-CNN | 0.961 | 0.920 | 需多数类与强基线复核 |
| Wafer | LSTM | 0.894 | 1.000 | 可能多数类行为，排除 |
| ElectricDevices | 1D-CNN | 0.551 | 0.113 | victim 未达质量门槛，排除 |
| ElectricDevices | LSTM | 0.381 | 0.411 | 欠训练，排除 |

## 5.4 匹配经典 clone 的对照：当前没有量子优势

表 4 是更有诊断价值的合成图像和长时间序列实验。在三个可比较单元中，独立 QNN 都显著落后于容量更高的经典 MLP clone。长序列 1D-CNN 上 QNN 仅比 Logistic 高 0.011，但比 MLP 低 0.441；这不是量子增益，而是弱线性基线与非线性基线之间的正常差异。

**表 4：256 查询下的 QNN 与经典 clone 对照。**

| 数据集 | Victim | Victim 准确率 | QNN | Logistic | MLP clone | QNN-MLP | 结论 |
|---|---|---:|---:|---:|---:|---:|---|
| 16x16 合成图像 | CNN | 1.000 | 0.617 | 1.000 | 1.000 | -0.383 | QNN 明显落后 |
| 长时间序列 | 1D-CNN | 1.000 | 0.559 | 0.548 | 1.000 | -0.441 | 无量子优势 |
| 长时间序列 | LSTM | 0.756 | 0.575 | 0.865 | 0.917 | -0.341 | 无量子优势 |

这张表应成为论文叙事的转折点：我们不再把“独立 QNN clone”视为主方法，而将研究问题调整为“QNN 能否在相同预算下改善**样本选择**”。只有 `QNN-guided MLP - Classical-active MLP` 的配对置信区间显著为正，才值得保留量子查询代理这一贡献。

## 5.5 LoRA 参数恢复：当前不支持参数窃取结论

低秩 LoRA toy 实验中，victim 准确率为 0.709，QNN 一致率为 0.505，投影后模型一致率为 0.676；但低秩更新余弦相似度只有 0.041、相对误差为 1.044。换言之，投影后的功能可能接近某些标签行为，但并未恢复私有 LoRA 参数。论文不得使用“窃取 LoRA 权重”或“参数逆向”表述，只能将其列为失败的探索性附录。

**表 5：LoRA toy 参数恢复诊断。**

| Victim 准确率 | QNN 一致率 | 投影模型一致率 | 更新余弦相似度 | 相对误差 | 结论 |
|---:|---:|---:|---:|---:|---|
| 0.709 | 0.505 | 0.676 | 0.041 | 1.044 | 未恢复参数 |

## 5.6 噪声 replay：仅支持线路稳定性，不支持真机可用性

受控密度矩阵/有限 shots replay 的解析模拟器一致率为 0.810；在 2048 shots、相位翻转 0.002 下为 0.813，在 1024 shots、相位翻转 0.01 与振幅衰减 0.01 下分别为 0.793 和 0.795。这说明该**特定受控线路**在小噪声下没有立即崩溃，但它不是针对完整硬标签抽取任务的 IBM 真机实验，也没有 QPU job ID。正确结论只能是“为 NISQ replay 提供工程可行性信号”。

**表 6：有限 shots 含噪 replay（模拟器）。**

| Shots | 噪声 | 概率 | 解析一致率 | Replay 一致率 | Replay/解析 |
|---:|---|---:|---:|---:|---:|
| analytic | 无 | 0 | 0.810 | 0.810 | 1.000 |
| 2048 | phase flip | 0.002 | 0.810 | 0.813 | 0.998 |
| 1024 | phase flip | 0.010 | 0.810 | 0.793 | 0.968 |
| 1024 | amplitude damping | 0.010 | 0.810 | 0.795 | 0.975 |
| 512 | depolarizing | 0.005 | 0.810 | 0.793 | 0.958 |

## 5.7 可发表结论与下一步

当前可以发表在论文中的事实只有三点：第一，直接使用小型 QNN 作为高保真硬标签 clone 在多个已保存实验中不具竞争力；第二，单纯增加比特并不稳定改善性能；第三，受控有限 shots replay 显示小噪声下存在可测稳定性。当前**不能**发表的结论包括 QNN 查询优势、LoRA 参数窃取成功、QEDG 优于/劣于本方法、真实 IBM QPU 可用性和防御绕过。

下一轮主实验应优先生成四张真正的主结果图：`F-Q` 曲线、QNN-guided 相对 Classical-Active 的配对森林图、量子容量/噪声热图、以及防御强度-一致率-成本三维折线图。所有图必须从 multi-seed CSV 自动生成，并将失败单元保留在附录。
